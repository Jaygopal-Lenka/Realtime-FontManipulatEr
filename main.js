difference = 0;
rightWristX = 0;
leftWristX = 0;

  function setup() {
  video = createCapture(VIDEO);
  video.size(550, 500);

  canvas = createCanvas(800, 500);
  canvas.position(560,100);
}

function draw() {
background('#6C91C2');
}
